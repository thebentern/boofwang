<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>boofwang</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!--
		Declared here rather than answered per build in App Store Connect, so
		that one answer covers every upload and its basis is in version
		control. No cryptographic algorithm is implemented in this app.

		What it does do, none of which is encryption: SHA-256 over an image
		for integrity, in lib/codec/checksum.ts; a fixed four-byte XOR
		against the public constant "CO 7" on the UV-5R Mini's wire
		protocol, which lib/radios/uv5rmini/protocol.ts calls not encryption
		in as many words; and storing AES-256 key material as opaque bytes
		in a DM-32UV codeplug, which the radio encrypts with and this app
		never does. HTTPS to the repeater directories is the exemption every
		app relies on.
	-->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>LSSupportsOpeningDocumentsInPlace</key>
	<true/>
	<!--
		Not because boofwang wants a camera or a photo library. Opening a
		codeplug uses a plain file input, and app/components/OpenCodeplugButton
		drops its `accept` list inside a phone shell - iOS maps the list to
		document types and greys out every .bwp, which is the file this app
		exists to open. With no list, iOS offers Photo Library and Take Photo
		or Video alongside Choose File, and touching either without a usage
		string is not an error the app can catch: TCC terminates the process.

		That is what App Review hit on an iPad Air on 8 September 2026 -
		"the app crashed when we attempted to take a photo" - and it is the
		crash the automated pass had reported four days earlier without
		saying how.

		A photo is not a codeplug and boofwang will say so. The better fix is
		to declare .bwp as an exported UTI so the `accept` list can come back
		and the media sources never appear; that is a change to the one path
		that opens files, and it wants its own testing rather than being
		bundled into a rejection fix.
	-->
	<key>NSCameraUsageDescription</key>
	<string>iOS offers the camera when you open a codeplug file. boofwang cannot read a photograph, and does not use the camera for anything else.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>iOS offers your photo library when you open a codeplug file. boofwang cannot read a photograph, and does not read your library for anything else.</string>
	<key>NSBluetoothAlwaysUsageDescription</key>
	<string>boofwang reads and programs radios over Bluetooth.</string>
	<key>UIFileSharingEnabled</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
</dict>
</plist>
